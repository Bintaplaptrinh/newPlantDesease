# Docker-focused requirements
# Force CPU-only PyTorch wheels on Linux to avoid pulling CUDA (~GBs)
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

torch
torchvision
numpy
flask
flask-cors
pillow

# Needed for YOLOv11 detection (ultralytics) and image preprocessing (cv2)
opencv-python-headless
ultralytics
